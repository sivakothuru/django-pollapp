language: python
python:
  - "2.6"
  - "2.7"
# command to install dependencies
install: "pip install -r requirements.txt --use-mirrors"
# command to run tests
script: 
  - coverage run django_poll manage.py test  --settings=mysite.travis_settings

after_success:
  - coveralls